{%extends 'TransBaseBundle:Trans:index.html.twig'%}
{%block title%} Добавление авто {%endblock%}

{%block content%}
    <form action="{{ path(paths,{'car_id':car_id}) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">Редактировать</button>
    </form>
{%endblock%}
